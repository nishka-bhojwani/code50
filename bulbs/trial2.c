#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    int num = 65;
    for(int i = 0 ; i < 8; i ++)
    {
    int result= pow(2,i);
    int rem1= 72%(result);
    rem1
    printf("%i . ", rem1);
    }
}