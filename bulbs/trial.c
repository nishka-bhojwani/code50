#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string word = get_string(" Message: ");
    int length = strlen(word);
    for( int i = 0 ; i < length ; i++)
    {
        
    }

}