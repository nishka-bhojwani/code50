#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string word = get_string(" Message: ");
    int length = strlen(word);
    int rem[8];
    for( int i = 0 ; i < length ; i++)
    {
        (int) word[i] = int num;
        rem1= num%2;
        num =/ 2 ;
    }


}