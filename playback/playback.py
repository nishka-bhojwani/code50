# to write a programme that replaces space with ...
message  = str(input("message: "))
# creating a new variable that replaces the whitespaces with ...
new = message.replace(" ","...")
#printing the new function 
print(new)

