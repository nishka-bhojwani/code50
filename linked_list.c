#include <stdio.h>
#include <stdlib.h>

typedef struct node
{
    int number;
    struct node *next ;
}
node

int main (int argc ; char* argv[])
{

    node n = malloc(sizeof(node));

    node->number = 1;
    node ->


}
